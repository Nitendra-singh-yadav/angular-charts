import { IntTransformPipe } from './int-transform.pipe';

describe('IntTransformPipe', () => {
  it('create an instance', () => {
    const pipe = new IntTransformPipe();
    expect(pipe).toBeTruthy();
  });
});
